 <!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.saylaughs.crazysaylaugh.Dao.LikeMapper">

    <insert id="giveLike" parameterType="say_like">
        insert into say_like(user_id,a_id) values(#{say_like.user_id},#{say_like.a_id});
    </insert>

    <update id="delLike" >
        delete from say_like where user_id=#{user_id} and a_id=#{a_id};
    </update>

    <select id="queryLike" resultType="int">
        select count(1) from say_like where user_id=#{user_id} and a_id=#{a_id};
    </select>

    <select id="queryUserArt" resultType="say_like">
        select * from say_like where  user_id=#{user_id};
    </select>
</mapper>